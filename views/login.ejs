<!DOCTYPE html>
   <html lang="en">
   <head>
      <title>login form</title>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css">
      <link rel="stylesheet" href="L-R.css">
   </head>
   <style>
    @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@400;500&display=swap");
    @import url("https://www.google.com/url?sa=i&url=https%3A%2F%2Fwww.freepik.com%2Ffree-photos-vectors%2Ftravel-wallpaper&psig=AOvVaw3bug1-EXlwu9CM2x1auGS0&ust=1727224467661000&source=images&cd=vfe&opi=89978449&ved=0CBQQjRxqFwoTCPjvp-eq2ogDFQAAAAAdAAAAABAE");

:root {
  --white-color: hsl(0, 0%, 100%);
  --black-color: hsl(0, 0%, 0%);
  --body-font: "Poppins", sans-serif;
  --h1-font-size: 1.75rem;
  --normal-font-size: 1rem;
  --small-font-size: .813rem;
  --font-medium: 500;
}
* {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}
body,
input,
button {

  font-size: var(--normal-font-size);
  font-family: var(--body-font);
}
body {
  background-image: url("https://www.google.com/url?sa=i&url=https%3A%2F%2Fwww.freepik.com%2Ffree-photos-vectors%2Ftravel-wallpaper&psig=AOvVaw3bug1-EXlwu9CM2x1auGS0&ust=1727224467661000&source=images&cd=vfe&opi=89978449&ved=0CBQQjRxqFwoTCPjvp-eq2ogDFQAAAAAdAAAAABAE");
  color: var(--white-color);
}
input,
button {
  border: none;
  outline: none;
}
a {
  text-decoration: none;
}
img {
  max-width: 100%;
  height: auto;
}
.page {
  position: relative;
  height: 100vh;
  display: grid;
  align-items: center;
  background-color: black;
}
.img {
  position: absolute;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}
.form {
  position: relative;
  background-color: hsla(0, 0%, 10%, 0.1);
  border: 2px solid var(--white-color);
  margin-inline: 1.5rem;
  padding: 2.5rem 1.5rem;
  border-radius: 1rem;
  backdrop-filter: blur(8px);
}
.title {
  text-align: center;
  font-size: var(--h1-font-size);
  font-weight: var(--font-medium);
  margin-bottom: 2rem;
}
.content, .box {
  display: grid;
}
.content {
  row-gap: 1.75rem;
  margin-bottom: 1.5rem;
}
.box {
  grid-template-columns: max-content 1fr;
  align-items: center;
  column-gap: 0.75rem;
  border-bottom: 2px solid var(--white-color);
}
.icon, .eye {
  font-size: 1.25rem;
}
.input {
  width: 100%;
  padding-block: 0.8rem;
  background: none;
  color: var(--white-color);
  position: relative;
  z-index: 1;
}
.box-input {
  position: relative;
}
.label {
  position: absolute;
  left: 0;
  top: 13px;
  font-weight: var(--font-medium);
  transition: top 0.3s, font-size 0.3s;
}
.eye {
  position: absolute;
  right: 0;
  top: 18px;
  z-index: 10;
  cursor: pointer;
}
.box:nth-child(2) input {
  padding-right: 1.8rem;
}
.check, .check-group {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.check {
  margin-bottom: 1.5rem;
}
.check-label, .forgot, .register {
  font-size: var(--small-font-size);
}
.check-group {
  column-gap: 0.5rem;
}
.check-input {
  width: 16px;
  height: 16px;
}
.forgot {
  color: var(--white-color);
}
.forgot:hover {
  text-decoration: underline;
}
.button {
  width: 100%;
  padding: 1rem;
  border-radius: 0.5rem;
  background-color: var(--white-color);
  font-weight: var(--font-medium);
  cursor: pointer;
  margin-bottom: 2rem;
}
.x {
  text-align: center;
}
.x a {
  color: var(--white-color);
  font-weight: var(--font-medium);
}
.x a:hover {
  text-decoration: underline;
}
.input:focus + .label {
  top: -12px;
  font-size: var(--small-font-size);
}
.input:not(:placeholder-shown).input:not(:focus) + .label {
  top: -12px;
  font-size: var(--small-font-size);
}
@media screen and (min-width: 576px) {
  .page {
    justify-content: center;
  }
  .form {
    width: 432px;
    padding: 4rem 3rem 3.5rem;
    border-radius: 1.5rem;
  }
  .title {
    font-size: 2rem;
  }
}

   </style>
   <body>
      <div class="page">
         <img src="LR-bg.jpg" class="img">
         <form action="/" method="POST" class="form">
            <h1 class="title">Login</h1>
            <div class="content">
               <div class="box">
                  <i class="ri-user-3-line icon"></i>
                  <div class="box-input">
                     <input type="email" required class="input" id="email" placeholder=" ">
                     <label for="email" class="label">Email</label>
                  </div>
               </div>
               <div class="box">
                  <i class="ri-lock-2-line icon"></i>
                  <div class="box-input">
                     <input type="password" required class="input" id="pass" placeholder=" ">
                     <label for="pass" class="label">Password</label>
                     <i class="ri-eye-off-line eye" id="eye"></i>
                  </div>
               </div>
            </div>
            <div class="check">
               <div class="check-group">
                  <input type="checkbox" class="check-input" id="check">
                  <label for="check" class="check-label">Remember me</label>
               </div>
               <a href="#" class="forgot">Forgot Password?</a>
            </div>
            <button type="submit" class="button">Login</button>
            <p class="x">
               Don't have an account? 
               <a href="/register.html">Register</a>
            </p>
         </form>
      </div>
      <script src="L-R.js"></script>
   </body>
</html>